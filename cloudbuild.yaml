steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/partyscout-backend:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/partyscout-backend:latest'
      - '.'

  # Push the image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/partyscout-backend:$COMMIT_SHA']

  # Deploy to canary (us-east1)
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'partyscout-backend-canary'
      - '--image'
      - 'gcr.io/$PROJECT_ID/partyscout-backend:$COMMIT_SHA'
      - '--region'
      - 'us-east1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--memory'
      - '1Gi'
      - '--update-secrets'
      - 'FIREBASE_SERVICE_ACCOUNT_JSON=firebase-service-account-json:latest,GOOGLE_PLACES_API_KEY=google-places-api-key:latest'
      - '--update-env-vars'
      - 'FIREBASE_PROJECT_ID=$PROJECT_ID'

options:
  logging: CLOUD_LOGGING_ONLY

images:
  - 'gcr.io/$PROJECT_ID/partyscout-backend:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/partyscout-backend:latest'
